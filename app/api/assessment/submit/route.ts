import { NextResponse } from "next/server"; import { finishAttempt } from "@/lib/services/assessment"; export async function POST(req:Request){try{const b=await req.json();const result=await finishAttempt(b.attempt_id);return NextResponse.json(result,{status:result.error?.status??(result.error?400:200)});}catch{return NextResponse.json({data:null,error:{message:"Invalid request."}},{status:400});}}