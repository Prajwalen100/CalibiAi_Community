import { NextResponse } from "next/server"; import { recordAnswer } from "@/lib/services/assessment"; export async function POST(req:Request){try{const b=await req.json();const result=await recordAnswer(b.attempt_id,b.question_id,b.selected_index);return NextResponse.json(result,{status:result.error?.status??(result.error?400:200)});}catch{return NextResponse.json({data:null,error:{message:"Invalid request."}},{status:400});}}